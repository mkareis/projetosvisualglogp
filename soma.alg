algoritmo "soma"
var
   x, y:inteiro
procedimento soma(a, b: inteiro)
inicio
   escreval("Recebi o valor: ", a)
   escreval("Recebi o valor: ", b)
   escreva("A soma de ", a, " +", b, " é igual a ", a+b)
fimprocedimento
inicio
   x<- 5
   y<- 3
   soma(x,y)
fimalgoritmo
